server:
  port: 8081

  spring:
    datasource:
        url: jdbc:mysql://localhost:3306/user_sevice
        username: root
        password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
